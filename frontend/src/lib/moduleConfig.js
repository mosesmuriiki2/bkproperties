/**
 * CODE-LEVEL MODULE VISIBILITY CONFIG
 * Set to `false` to hard-disable a module from the codebase.
 * If `false`, the module will be hidden regardless of admin settings.
 * If `true`, the admin panel toggle still applies.
 */
export const MODULE_CONFIG = {
  hotels: true,
  tours: true,
  cars_sale: true,
  cars_hire: true,
  properties_sale: true,
  properties_rent: true,
  land: true,
  tourist_vehicles: true,
};